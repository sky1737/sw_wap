/**
 * Created by zxjBigPower on 2017/6/10.
 */
$(function () {
    /*showFile($("#splt")[0],$(".filebox-box")[0])
     showFile($("#scxk")[0],$(".filebox-box")[0])
     showFile($("#yyzz1")[0],$(".filebox-box")[0])
     showFile($("#yyyzz2")[0],$(".filebox-box")[0])
     showFile($("#yyzz3")[0],$(".filebox-box")[0])*/
    /*
     * 选择照片并显示
     * file：file文件选择，show：展示页
     * */
/*    function showFile(file,show) {
            //console.log(1);
            //创建对象
            var reader = new FileReader();
            //console.log(this.files[0])
            reader.readAsDataURL(this.files[0]);
            reader.onload = function () {
                //console.log(reader.result)
                show.style.background = "url(" + reader.result + ") no-repeat center/contain";
                file.parentNode.parentNode.childNodes[1].style.backgroundColor="#fff";
            }

    }
    showFile($("#yyzz")[0], $(".filebox-box")[0],0)
    showFile($("#swdj")[0], $(".filebox-box")[1],1)
    showFile($("#sfz")[0], $(".filebox-box")[2],2)
    showFile($("#zhzm")[0], $(".filebox-box")[3],3)
    showFile($("#mgcz")[0], $(".filebox-box")[4],4)*/

})